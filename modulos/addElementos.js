
function cambiarValores(datosArray, index) {

    // push agregar uno o más elementos al final de un arreglo (array).
    datosArray.push(index);    
    return  datosArray
  }
    // Con
    figuración
  let testArr = [1, 2, 3, 4, 5];
  
 
 console.log("Mis arreglo original: " + JSON.stringify(testArr));
 console.log(cambiarValores(testArr, 65));
 
